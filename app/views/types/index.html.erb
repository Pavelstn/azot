<h1>Listing types</h1>

<table>
  <tr>
    <th>Active</th>
    <th>Order</th>
    <th>Name</th>
    <th>Aliasname</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @types.each do |type| %>
  <tr>
    <td><%= type.active %></td>
    <td><%= type.order %></td>
    <td><%= type.name %></td>
    <td><%= type.aliasname %></td>
    <td><%= link_to 'Show', type %></td>
    <td><%= link_to 'Edit', edit_type_path(type) %></td>
    <td><%= link_to 'Destroy', type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Type', new_type_path %>
